name: SafeFlame

options:
  bundleIdPrefix: com.undu
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true

targets:
  SafeFlame:
    type: application
    platform: iOS
    sources:
      - path: SafeFlame/SafeFlameApp.swift
      - path: SafeFlame/ContentView.swift
      - path: SafeFlame/CandleController.swift
    resources:
      - path: SafeFlame/Resources/fireplace.wav
      - path: SafeFlame/Resources/candle.mp3
      - path: SafeFlame/Resources/nightlight.wav
      - path: SafeFlame/Resources/CC0-NOTICE.txt
    info:
      path: SafeFlame/Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.undu.safeflame
        PRODUCT_NAME: SafeFlame
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        INFOPLIST_KEY_CFBundleShortVersionString: "1.0"
        INFOPLIST_KEY_CFBundleVersion: "1"
        SWIFT_VERSION: 5.0
        TARGETED_DEVICE_FAMILY: "1"
        CODE_SIGN_STYLE: Manual
